const INTERCOM_MODULE = Object.freeze({
  enabled: false,
  entityIds: Object.freeze({}),
  capabilities: Object.freeze({
    connection: true,
    call: true,
    camera: true,
    unlock: true,
    eventHistory: true,
  }),
});
